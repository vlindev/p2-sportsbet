# AGENTS.md — Project Agent Instructions

This repo's canonical project instructions live in `CLAUDE.md`.

The agent reading this file should:
- Read and follow `CLAUDE.md` before project work.
- Treat `CLAUDE.md` as the shared source of truth for project rules, codewords, memory protocol, security rules, and workflow standards.
- Apply your runtime's native tool behavior where Claude-specific tooling is referenced (`Bash`, `Read`, `Edit`, etc.); the rule, not the tool name, is what binds.
- Do not edit `AGENTS.md` to override, supplement, or duplicate `CLAUDE.md`. If a rule needs to change, update `CLAUDE.md`.
